#pragma once

#include <godot_cpp/classes/object.hpp>

namespace godot {
    class AIAgent: public Object
    {
    private:
        /* data */
    public:
        AIAgent(/* args */);
        ~AIAgent();
    };    
}