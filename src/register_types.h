/* Register types for the MiniBrainGd extension */
#pragma once

#include <godot_cpp/godot.hpp>

using namespace godot;

void initialize_minibrain_module(ModuleInitializationLevel p_level);
void uninitialize_minibrain_module(ModuleInitializationLevel p_level);
